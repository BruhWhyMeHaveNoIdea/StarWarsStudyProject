# SWAPI_for_students
Star Wars api app for students practice

### 📦 Установка

1. Клонирование репозитория

    ```bash
    git clone https://github.com/VadimStr315/SWAPI_for_students.git
    cd SWAPI_for_students
    cd api
    ```

2. Создание виртуального окружения

    Windows:

    ```bash
    python -m venv venv
    venv\Scripts\activate
    ```

    Linux/Mac:

    ```bash
    python3 -m venv venv
    source venv/bin/activate
    ```

3. Установка зависимостей

    ```bash
    pip install -r requirements.txt
    ```

    Если файла requirements.txt нет, установите зависимости вручную:

    ```bash
    pip install fastapi sqlalchemy pydantic uvicorn
    ```

### Структура проекта

```text
    api/
    ├── main.py              # Основной файл приложения
    ├── database.py          # Настройка базы данных
    ├── models.py            # SQLAlchemy модели
    ├── schemas.py           # Pydantic схемы
    ├── crud.py              # CRUD операции
    ├── init_db.py           # Скрипт заполнения БД
    ├── routers/             # Роутеры API
    │   ├── __init__.py
    │   ├── characters.py
    │   ├── films.py
    │   ├── starships.py
    │   └── vehicles.py
    └── starwars.db          # Файл базы данных (создается автоматически)
```

### 🚀 Запуск

1. Инициализация базы данных

    ```bash
    python init_db.py
    ```

    Это создаст файл starwars.db и заполнит его данными:

    - 12 фильмов
    - 82 персонажа
    - 25 космических кораблей
    - 30 транспортных средств

2. Запуск сервера

    ```bash
    uvicorn main:app --reload
    ```

    Сервер будет доступен по адресу: http://localhost:8000

3. Проверка работоспособности
    Откройте браузер и перейдите по одному из адресов:

    Главная страница: http://localhost:8000

    Swagger документация: http://localhost:8000/docs

    ReDoc документация: http://localhost:8000/redoc
