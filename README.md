# StarWarsStudyProject

Star Wars wiki-site for study homework

## 📦 Установка

### Клонирование репозитория

```bash
git clone https://github.com/BruhWhyMeHaveNoIdea/StarWarsStudyProject.git
cd StarWarsStudyProject
```

### Настройка Docker

Убедитесь, что Docker установлен и запущен на вашей системе.

## 🚀 Запуск

### Запуск приложения

```bash
docker-compose up --build
```

После завершения сборки, сервер будет доступен по адресу: **http://localhost:8000**

### Проверка работоспособности

Откройте браузер и перейдите по одному из адресов:

- SWapi страница: **http://localhost:8000**
- SWapi Swagger документация: **http://localhost:8000/docs** (если используется FastAPI)
- SWapi ReDoc документация: **http://localhost:8000/redoc** (если используется FastAPI)

- Главный сайт **http://localhost:8001**

## 🔧 Технологии

- **FastAPI** - веб-фреймворк для создания API для SWapi
- **SQLAlchemy** - ORM для работы с базой данных
- **SQLite** - база данных (может быть заменена на PostgreSQL в Docker)
- **Docker** - контейнеризация приложения
- **Docker Compose** - оркестрация контейнеров
- **Django** - веб-фреймворк для создания основного сайта

## 📚 Дополнительная информация

Для остановки приложения используйте:

```bash
docker-compose down
```

Для просмотра логов:

```bash
docker-compose logs -f
```

Для пересборки без кэша:

```bash
docker-compose build --no-cache
```
